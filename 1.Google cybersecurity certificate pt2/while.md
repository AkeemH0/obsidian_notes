```
time=0
while time<=10:
#the variable time needs to be created outside of the loop (cannot be created inside of the loop like if statements) & needs to be incremented inside the loop e.g. i+=1

```