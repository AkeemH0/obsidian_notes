A software component within a device (computer or network router) that initiates DNS queries to resolve domain names to IP addresses.
Can be either:
- [[recursive DNS resolver]] 
- [[Iterative DNS resolver]]