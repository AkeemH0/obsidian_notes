[[cookies]] that can only be sent over an encrypted connection e.g. [[HTTPS (Hypertext Transfer Protocol Secure)]].
Sensitive information shouldn't be saved in a cookie as it can easily be read from the browser.
